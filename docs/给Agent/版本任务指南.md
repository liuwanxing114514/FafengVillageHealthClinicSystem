# Cursor 版本任务指南

本文档说明：**每个版本开新对话时，需要给 Cursor 什么材料、怎么下指令、怎么验收**。  
诊所机器上的升级操作见 [`../共用/DEPLOYMENT.md`](../共用/DEPLOYMENT.md)。给人看的白话说明见 [`../给人看/后续版本操作说明.md`](../给人看/后续版本操作说明.md)。版本定义见 [`../共用/ROADMAP.md`](../共用/ROADMAP.md)。

---

## 使用方式

1. **开新对话**（或新 Agent 会话），避免上下文过长混乱。
2. **用 @ 引用**下面「必读文件」中的文档和关键代码目录。
3. **复制对应版本的 Prompt**，填好 `【】` 中的实际信息。
4. 要求 Cursor **一次只做一个版本**，完成后对照验收清单。
5. 验收通过后再做下一版；诊所上线按 [`../共用/DEPLOYMENT.md`](../共用/DEPLOYMENT.md) 与 [`../给人看/后续版本操作说明.md`](../给人看/后续版本操作说明.md) 执行。

---

## 每次都要给 Cursor 的通用材料

无论哪一版，建议始终 @ 引用：

| 文件 | 路径 |
| --- | --- |
| 路线图 | `docs/共用/ROADMAP.md` |
| 开发交接 | `docs/给Agent/开发交接.md` |
| 业务大纲 | `docs/共用/大纲.md` |
| 部署升级 | `docs/共用/DEPLOYMENT.md` |
| 使用者需求 | `docs/给人看/使用者功能确认.md` |
| AI 架构 | `docs/给Agent/AI架构.md` |

若项目已有代码， additionally @：

| 路径 | 原因 |
| --- | --- |
| `docker-compose.yml` | 服务编排 |
| `.env.example` | 配置项约定 |
| `backend/` 相关模块包 | 避免重复造轮子 |
| `frontend/src/` 相关页面 | 保持 UI 一致 |
| 最近一次 Flyway 迁移脚本 | 数据库变更衔接 |

### 通用约束（可贴在每次 Prompt 末尾）

```text
必须遵守：
- 模块化单体，不做微服务（AI/OCR/Whisper 可独立容器，业务仍在一个 backend）。
- 库存变更必须走流水；禁止直接改库存数量。
- 出库关联患者和处方；库存不足阻止出库；出库批次由系统 FEFO 推荐，用户手动确认后才扣减（可改选）。
- AI/Agent 只能写 ai_draft 草稿，人工确认后才写正式数据。
- 核心数据软删除，保留审计。
- 金额/数量不用 float；Flyway 管理数据库变更。
- 一次只完成当前版本，不要顺带做下一版本功能。
- 完成后说明：改了哪些文件、如何验证、需要更新 `.env.example` 和 `docs/共用/DEPLOYMENT.md` 哪些项。
```

---

## 第一期：v0.1 – v1.0（从零到可部署）

### 何时用

项目还没有代码，或要重新开始第一期。

### 必读文件

- `docs/共用/大纲.md`
- `docs/给人看/使用者功能确认.md`
- `docs/共用/ROADMAP.md`（v0.1–v1.0 章节）
- `docs/给Agent/开发交接.md`

### Prompt（任务 0：只做设计）

```text
请先阅读 @docs/共用/ROADMAP.md @docs/共用/大纲.md @docs/给人看/使用者功能确认.md @docs/给Agent/开发交接.md

本项目是发凤村卫生室诊所辅助系统。采用模块化单体，第一版 v0.1–v1.0。

当前任务：只做设计，不写代码。

请输出：
1. 项目目录结构（backend / frontend / scripts / docker-compose）
2. 后端包结构（common / system / medicine / inventory / patient / clinic / ai / agent）
3. 前端目录结构
4. 核心数据库表设计草案（含 ai_draft 预留）
5. v0.1–v1.0 任务拆分（对齐 ROADMAP）
6. 风险与待确认问题

【通用约束见 docs/给Agent/版本任务指南.md】
```

### Prompt（任务 1 起：按 ROADMAP 逐任务开发）

```text
请先阅读 @docs/共用/ROADMAP.md @docs/给Agent/开发交接.md @docs/共用/DEPLOYMENT.md

当前项目状态：【例如：v0.3 药品管理已完成，数据库已有 medicine 表】

当前任务：实现 ROADMAP 中的【v0.4 患者和门诊病历】。

开始前请先简短说明：
- 涉及哪些表、接口、页面
- 与已有模块的依赖

确认设计后再编码。完成后提供验证步骤。

【通用约束见 docs/给Agent/版本任务指南.md】
```

### 第一期验收

对齐 `docs/共用/ROADMAP.md` v1.0「交付」+ `docs/共用/DEPLOYMENT.md` 第八节 v1.0 基线清单。

---

## v1.1 语音输入

### 前置条件

- v1.0 已部署稳定
- v0.9 的 `ai` 模块、`uploads/audio` 目录已存在

### 额外 @ 引用

- `backend/.../ai/`（AiProvider 等）
- `frontend/.../` 病历录入页
- `docker-compose.yml`

### 需告知 Cursor 的信息

```text
- Whisper 部署方式：Docker 容器 whisper-service
- 音频格式偏好：【如 webm / wav】
- 是否接受浏览器 MediaRecorder API
```

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.1 章节）@docs/给Agent/开发交接.md @docs/共用/DEPLOYMENT.md

当前版本：v1.0 已上线。
当前任务：实现 v1.1 语音输入。

范围：
- 病历录入页字段旁增加语音按钮
- 前端录音 → 上传 backend → 转发 Whisper 服务 → 转写文本填入当前字段
- docker-compose 增加 whisper-service（可用 profile 可选启用）
- .env.example 增加 CLINIC_WHISPER_URL 等配置
- 转写结果不得自动保存病历

不做：流式转写、自动保存、Whisper 不可用时拖垮整个系统（应友好降级）。

请先给设计（接口、容器、前端交互），再编码。

【通用约束】
```

### 验收

- `docs/共用/DEPLOYMENT.md` v1.1 清单
- 停止 whisper 容器后，病历手动录入仍正常

---

## v1.2 快捷语候选

### 前置条件

- v1.0 已部署（不依赖 v1.1）

### 额外 @ 引用

- 病历录入相关 frontend/backend
- 现有 `clinic_visit` 表结构

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.2 章节）@docs/给Agent/开发交接.md

当前版本：【v1.0 或 v1.1】已上线。
当前任务：实现 v1.2 快捷语候选。

范围：
- 新建快捷语表（按字段分类：主诉、诊断、处理意见、用法等）
- 录入页展示候选，点击填入
- 根据历史输入频率排序（可先简单统计 clinic_visit 字段）
- 快捷语管理 CRUD 页面
- 可选：低频候选清理配置

不做：AI 自动生成快捷语。

请先给表设计和 API 列表，再编码。

【通用约束】
```

---

## v1.3 DeepSeek 结构化整理

### 前置条件

- v0.9 `AiProvider`、`ai_draft` 表已存在
- 已决定脱敏规则（ROADMAP T1）

### 需告知 Cursor 的信息

T1 已定稿，见 `@docs/给Agent/AI架构.md` 第 4 节。摘要：

```text
脱敏规则（部分屏蔽，出站 API 前执行；库内和确认页仍用原文）：
- 姓名：留姓，名用 *（张三→张*，欧阳娜娜→欧阳**）
- 手机：前3+****+后4（13812345678→138****5678）
- 住址：留村/组/路/街，只屏蔽门牌/栋/单元/室（发凤村3组12号→发凤村3组**号）
- 身份证：前6+********+后4
- OCR 单据：联系人、电话、门牌、身份证同样脱敏；药名/数量/批号/有效期可原文发
- 实现 Desensitizer 工具类 + 单元测试
```

### 额外 @ 引用

- `backend/.../ai/` 全部
- `ai_draft` 相关 migration 与 entity
- `.env.example`

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.3 章节）@docs/给Agent/开发交接.md @docs/给Agent/AI架构.md（若已有）

当前任务：实现 v1.3 DeepSeek 结构化整理。

范围：
- 实装 DeepSeekAiProvider（聊天/结构化 JSON 抽取）
- 实装 Desensitizer（规则见 docs/给Agent/AI架构.md §4，T1 已确认）
- 自由文本 / 语音转写文本 → 整理为主诉、现病史、诊断等 → 写入 ai_draft（VISIT 类型）
- 前端：草稿预览页，逐字段确认后写入 clinic_visit
- 提示词模板可配置（application.yml 或数据库表）
- clinic.ai.provider 支持 deepseek / noop 切换

不做：OCR（v1.4）、Agent 工具（v2.0）、未确认草稿写正式表。

请先更新 docs/给Agent/AI架构.md 中的脱敏与草稿流程说明，再编码。

【通用约束】
```

### 验收

- 草稿未确认前，数据库 `clinic_visit` 无新增
- `enabled=false` 或 `provider=noop` 无报错

---

## v1.4 OCR 入库

### 前置条件

- v1.3 DeepSeek 可用（整理表格）
- v0.6 入库、v0.7 扫码已稳定

### 需告知 Cursor 的信息

```text
- OCR 方案：PaddleOCR 独立容器
- 仅支持打印版清单/发票/送货单，不支持手写
- 图片存储：D:\clinic-data\uploads\images（或容器挂载 uploads/images）
```

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.4 章节）@docs/给Agent/开发交接.md @docs/共用/DEPLOYMENT.md

当前任务：实现 v1.4 OCR 入库。

范围：
- 入库页：上传/手机拍照图片
- backend 调 ocr-service 提取文字 → DeepSeek 整理为入库行列表 → ai_draft（INBOUND 类型）
- 前端待审核表格：增删改行 → 批准 → 调用现有入库逻辑批量生成流水
- docker-compose 增加 ocr-service
- .env.example 增加 CLINIC_OCR_URL

不做：手写识别、自动入库、条码 OCR。

批准前不得调用入库 Service。请先给流程图级设计再编码。

【通用约束】
```

---

## v1.5 批量出库

### 前置条件

- v0.6 单条出库稳定

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.5 章节）@docs/给Agent/开发交接.md

当前任务：实现 v1.5 批量出库。

范围：
- 出库页支持多药品选择、数量、单位
- 统一填写原因，二次确认
- 每条药品独立 inventory_flow，共用同一批次操作 ID 可选
- 库存不足时整单或单行阻止（请先说明策略并实现一致）

不做：Agent 自动批量出库。

【通用约束】
```

---

## v1.6 处方模板与就诊收费

### 前置条件

- 使用者已提供**纸质处方样本**（扫描件或照片）
- 分支 `v1.6-prescription-template` 基线含 v1.5

### 需告知 Cursor 的信息

```text
请 @ 或 附件提供：处方纸扫描图（已提供协会预印笺样本）
预印纸：费别/卡号/处方金额/调剂留空；科别「全科」；门诊病历号只印数字
就诊收费：病历加应收、实收；统计欠款；进患者详情时提醒
```

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v1.6 章节）@docs/给人看/使用者功能确认.md @docs/给Agent/功能点与实现记录.md

当前任务：实现 v1.6 处方模板与就诊收费。

范围：
A. 处方打印
- 双模板：default-a4（保留现有）+ preprinted-fafeng（预印纸叠加对齐）
- 可配置页眉、字段位置、字号；设置页切换模板
- 预印模式不打印纸上已有标签；日期拆年月日；Rp 下自由文本药品行

B. 就诊收费
- 药品建议零售价；应收默认 Σ(零售价×处方数量)，可手改；参考成本仅界面
- 手改后处方变更弹窗确认更新应收

C. 病历入口
- 菜单+列表+搜索；弹窗建患者不丢表单

请先对照 @docs/给Agent/功能点与实现记录.md 确认文档后再编码。

【通用约束】
```

---

## v2.0 Agent 自然语言查询

### 前置条件

- v1.3 DeepSeek 可用
- v0.9 agent 包、AI 助手占位页已存在

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v2.0 章节）@docs/给Agent/开发交接.md @docs/给Agent/AI架构.md

当前任务：实现 v2.0 Agent 自然语言查询。

范围：
- 实装受控工具（只读或写 ai_draft）：
  searchMedicine, queryInventory, queryExpiringMedicine,
  searchPatient, searchPatientVisit, generateOutboundDraft
- Agent 编排：意图识别 → 工具调用 → 结果摘要
- AI 助手页：展示计划、工具名、参数摘要、结果
- 会改数据的动作 → 待确认卡片，不直接执行
- agent_execution_log 表记录调用

禁止：直接 UPDATE 库存/病历、任意 SQL、未注册工具。

请先列出工具接口签名和 Agent 流程，再编码。更新 docs/给Agent/AI架构.md。

【通用约束】
```

---

## v2.1 流程自动化

### 前置条件

- v2.0 Agent 可用
- v0.5 处方、v0.6 出库稳定

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v2.1 章节）@docs/给Agent/开发交接.md

当前任务：实现 v2.1 处方→出库→打印流程。

范围：
- 处方保存后「生成待出库清单」→ ai_draft（OUTBOUND）
- 待确认页：核对药品、数量、批次 → 确认 → 批量出库（复用 v0.6/v1.5 逻辑）
- 成功后跳转处方打印
- 失败或取消：不半扣库存，草稿状态可恢复

请先说明事务边界和回滚策略，再编码。

【通用约束】
```

---

## v2.2 脱敏病历向量化

### 前置条件

- v1.0 已部署（**pgvector、pg_trgm 已在 v0.1 安装**，无需再装扩展）
- 有一定量历史病历

### 需告知 Cursor 的信息

```text
- 向量模型：【DeepSeek embedding / 本地模型 / 其他】
- 脱敏：与 docs/给Agent/AI架构.md §4 / T1 一致（姓名留姓、手机首尾、门牌屏蔽、身份证前6后4）
```

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v2.2 章节）@docs/给Agent/AI架构.md

当前任务：实现 v2.2 脱敏历史病历向量化。

范围：
- visit_embedding 表（pgvector）
- 脱敏 pipeline → 文本拼接 → 向量化 → 入库
- 管理接口：手动触发全量/增量同步
- 不向外部 API 发送未脱敏原文

不做：相似检索 UI（v2.3）。

【通用约束】
```

---

## v2.3 相似病例检索

### 前置条件

- v2.2 已有向量数据

### Prompt

```text
请先阅读 @docs/共用/ROADMAP.md（v2.3 章节）@docs/给Agent/开发交接.md

当前任务：实现 v2.3 相似病例检索。

范围：
- 病历录入页：根据当前主诉/现病史/诊断检索 Top-3
- 仅展示脱敏摘要
- 明显 UI 文案：「仅供参考，不替代诊断」
- 检索失败时不阻塞录入

【通用约束】
```

---

## 版本完成后 Cursor 应交付的文档更新

每完成一个版本，要求 Cursor 检查并更新：

| 文件 | 更新内容 |
| --- | --- |
| `docs/共用/ROADMAP.md` | 若交付物有微调，同步修订 |
| `.env.example` | 新增配置项 + 注释 |
| `docs/共用/DEPLOYMENT.md` | 该版本升级差异、验收项 |
| `docs/给人看/后续版本操作说明.md` | 使用者可见变化 |
| `docs/给Agent/AI架构.md` | v1.3 起 AI/Agent/OCR 相关变更 |
| `README.md`（项目根） | 新功能一句话说明 |

---

## 快速对照表

| 版本 | 主要 Prompt 关键词 | 诊所升级额外操作 |
| --- | --- | --- |
| v0.1–v1.0 | 设计 / 骨架 / Flyway / 业务模块 | 首次部署 |
| v1.1 | Whisper、语音按钮、audio 上传 | 加 whisper 容器 |
| v1.2 | 快捷语表、频率排序 | 仅换程序 |
| v1.3 | DeepSeekAiProvider、脱敏、ai_draft | 填 API Key |
| v1.4 | PaddleOCR、INBOUND 草稿、批准入库 | 加 ocr 容器 |
| v1.5 | 批量出库、二次确认 | 仅换程序 |
| v1.6 | 处方纸样本、打印模板、病历应收实收 | 仅换程序（Flyway V11） |
| v2.0 | Agent 工具、execution_log | 开 AI |
| v2.1 | 处方→出库流程、事务 | 仅换程序 |
| v2.2 | pgvector、embedding | 确认扩展 |
| v2.3 | 相似病例 Top-3 | 仅换程序 |

---

## 给 Cursor 的反面示例（不要这样说）

```text
❌ 「帮我把后续 AI 功能都做完」          → 范围失控
❌ 「顺便 refactor 一下整个 backend」    → 偏离版本
❌ 「不用 Flyway，直接改表」             → 破坏升级链
❌ 「Agent 直接帮用户出库吧」            → 违反安全规则
❌ 「不用备份，直接升级」                → 运维风险
```

---

## 相关文档

- [`../共用/DEPLOYMENT.md`](../共用/DEPLOYMENT.md) — 诊所机器升级命令
- [`../给人看/后续版本操作说明.md`](../给人看/后续版本操作说明.md) — 后续版本白话操作
- [`../共用/ROADMAP.md`](../共用/ROADMAP.md) — 版本权威定义
- [`开发交接.md`](开发交接.md) — 第一期总原则
- [`../README.md`](../README.md) — 文档总目录
