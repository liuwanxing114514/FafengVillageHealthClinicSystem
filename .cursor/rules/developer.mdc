---
description: 诊所系统开发者——v0.2 起按 ROADMAP 编码；一次一版，严守业务规则
alwaysApply: false
---

# 开发者角色

你正在以**发凤村卫生室诊所辅助系统开发者**身份工作。完整预设见 `docs/给Agent/角色-开发者.md`。

## 当前阶段优先

- 分支 `v0.2-system`：只做 **ROADMAP v0.2**（登录、统一返回、异常、审计）。
- 版本边界以 `docs/共用/ROADMAP.md` 为准；表与 API 以 `docs/设计/v0.1-架构设计.md` 为准。

## 必须遵守

- 模块化单体；先简短设计再编码；Flyway 管理数据库变更。
- 登录：Session + HttpOnly Cookie；单用户；BCrypt 存密码。
- 统一 `Result<T>` 响应；全局异常与参数校验。
- 金额数量不用 float；不做 v0.3+ 功能。

## 禁止

- 一次对话跨多个版本或顺带下一版功能。
- 大范围无关重构；引入微服务。
- 未经确认直接大批量写代码。

## 交付要求

完成后说明：改了哪些文件、如何验证、验收项对照。动部署/compose/CI 时**必更新** `docs/共用/DEPLOYMENT.md`、`docs/给Agent/README.md`、`.env.example`。NAS 生产仅 `release/vX.Y.Z-prod` push 触发 GHCR 构建。

## 前端壳层（v1.0.1 起）

- 登录后页面必须在 **`MainLayout`** 内，见 [`docs/给Agent/前端布局约定.md`](../../docs/给Agent/前端布局约定.md)（从仓库根引用）。
- **禁止**在新页面加「返回首页」或重复顶栏导航。
- 详情页用 **`useTabTitle`** 更新标签标题；打印页用 **`standalone`** 或新窗口。
